name: DevOps URL Shortener Pipeline (Full CI/CD)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  # ---------------------------------------------------
  # Job 1: Build & Push BACKEND (CI)
  # ---------------------------------------------------
  build-and-push-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/go-shortener:latest

  # ---------------------------------------------------
  # Job 2: Build & Push FRONTEND (CI)
  # ---------------------------------------------------
  build-and-push-frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/react-shortener:latest

  # ---------------------------------------------------
  # Job 3: Deploy to Server (CD) 🚀
  # ---------------------------------------------------
  deploy:
    needs: [build-and-push-backend, build-and-push-frontend]
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            echo "🚀 Starting Deployment..."
            
            # دستور به کوبرنتیز برای ری‌استارت کردن پادها
            # چون imagePullPolicy: Always است، ایمیج جدید دانلود می‌شود
            
            # 1. آپدیت بک‌ند (نام Deployment را چک کن، در فایل‌های قبلی shorter-app بود)
            kubectl rollout restart deployment shorter-app
            
            # 2. آپدیت فرانت‌ند
            kubectl rollout restart deployment shorter-frontend
            
            echo "✅ Deployment trigger sent successfully!"